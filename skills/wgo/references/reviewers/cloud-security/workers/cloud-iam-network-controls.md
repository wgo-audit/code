Inspect one cloud, IAM, and network evidence slice. Use the reviewer's topology
packet; do not invoke CodeGraph.

Return a concise evidence packet with exact file, console-export, command,
resource, policy, DNS, certificate, route, or hosted-source locators. Separate
observed, inferred, and unknown boundaries. Focus on accounts/projects, IAM,
service identities, network paths, ingress, segmentation, WAF, admin access,
and public exposure that could change the reviewer decision. Do not write audit artifacts or shared state.
